apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Fixed uid so provisioned dashboards can reference it verbatim.
    uid: hawtch-prom
    access: proxy
    url: http://hawtch-prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
